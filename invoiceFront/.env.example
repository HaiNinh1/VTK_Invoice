# Base URL for the Laravel API. In dev, leave as "/api" to use the Vite
# proxy configured in vite.config.js (forwards to http://localhost:8000).
# In production, set to the absolute backend URL, e.g.
#   VITE_API_BASE_URL=https://api.vtk-hoadon.viettel.vn/api
VITE_API_BASE_URL=/api
