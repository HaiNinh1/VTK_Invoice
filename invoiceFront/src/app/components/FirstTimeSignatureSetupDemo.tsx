import FirstTimeSignatureSetup from './FirstTimeSignatureSetup';

/**
 * Demo page to showcase the First-Time Signature Setup onboarding screen
 * This is a full-screen blocking overlay shown when a user logs in for the first time
 * and has no digital signature configured.
 */
export default function FirstTimeSignatureSetupDemo() {
  return <FirstTimeSignatureSetup />;
}
